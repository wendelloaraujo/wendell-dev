/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['via.placeholder.com'], // Adicione aqui o domínio de onde você está carregando as imagens
    },
};

export default nextConfig;
